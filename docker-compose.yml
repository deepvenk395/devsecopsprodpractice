services:
  web:
    build: .
    ports:
      - "4173:4173"
    environment:
      NODE_ENV: production
      PORT: 4173
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

pipeline {
    agent any

    stages {
        stage('checkout') {
            steps {
                echo "Repository checked out successfully!"
                sh 'pwd'
                sh 'ls -la'
            }
        }
    }
}